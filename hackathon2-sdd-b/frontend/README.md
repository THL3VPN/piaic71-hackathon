# Frontend scaffold (Next.js App Router)

This placeholder documents the intended structure for the Next.js 16+ App Router frontend with Better Auth:

- app/login/ — login page
- app/signup/ — signup page
- app/tasks/ — tasks dashboard
- lib/api.ts — API client attaching `Authorization: Bearer <token>`
- components/TaskForm.tsx, TaskList.tsx, TaskItem.tsx — CRUD UI components

Implement per Phase II plan; no code generated here yet.
