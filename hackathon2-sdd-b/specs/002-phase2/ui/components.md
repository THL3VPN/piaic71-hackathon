# Phase II UI Components

- **TaskList**: Renders tasks for authenticated user; supports mark complete; uses JWT-protected API.
- **TaskItem**: Displays title, status, actions (edit/delete/complete).
- **TaskForm**: Create/update task; fields align with schema; submits with JWT.
- **Filters**: Client-side filters consistent with API query behavior.
